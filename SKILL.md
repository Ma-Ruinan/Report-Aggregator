---
name: report-aggregator
description: 将多份测评/测试子报告整合为一份完整、统一、可自检的综合测评报告。适用于「多份同源测评子报告（各自关注不同能力维度）→ 一份综合报告」的任务。当用户需要把若干份测试/测评子报告汇总合成一份总体报告时使用。
description_zh: "将多份测评子报告整合为一份统一的综合测评报告"
description_en: "Aggregate multiple evaluation sub-reports into one unified comprehensive report"
version: "1.0.0"
author: "report-aggregator"
agent_created: true
---

# Report Aggregator — 测评子报告汇总方法论

把多份测评/测试子报告，整合为**一份完整、统一、无拼接感的综合测评报告**。本 skill 沉淀的是「怎么做、有什么硬性红线、如何自检」，而不是某一次具体任务的复刻。

## 何时使用本 skill

当任务满足以下特征时使用：

- 存在**多份子报告**，每份关注测评任务的**一个或几个能力维度**（如常规能力、系统性能、深度洞察、数据分析、安全防护等）。
- 目标是产出**一份综合报告**，把各子报告的结论整合为一个整体。
- 子报告的**数量、维度、测试对象、主题未来可能变化**，但「多份子报告 → 一份综合报告」这一任务本质不变。

不适用场景：与测评无关的泛化「多文档合并」任务（如合并会议纪要、拼凑资料），那些任务有各自不同的要求，应另建 skill。

## 核心原则（贯穿全程，不可违背）

1. **数值忠实**：子报告中出现的任何数值、结论，只能忠实转述，不得凭个人理解加工、修改、编造、偏移。整合的意义就在于忠实。
2. **口径诚实**：不同维度/不同测试的评分尺度、样本规模、上游条件若不同，**不得强行合成单一分数或单一结论**；如实说明口径差异。
3. **图表能留则留**：子报告中的图表（柱状图、折线图、矩阵等）只要没有明显错误，都应保留进综合报告；仅以「整体性、合理性」为前提做样式级调整（配色、字号、图注编号），不动图内数据。
4. **判断交还制作人**：执行者按理解整理后，对「拿不准的处理点」（如源报告自身的数值矛盾、某图是否裁剪、口径是否统一）应**列出来让制作/负责人判断**，而非擅自决定。
5. **强制自检**：综合报告生成后，必须拿成品回源比对子报告做自检，而不是「做完就发」。自检往往能发现问题。
6. **表述可读**：报告语言要有专业感，但必须让普通读者能理解；不用抽象到需要揣测的词汇。

## 执行流程（五阶段）

按顺序执行，每阶段完成后自检，再进入下一阶段。详细方法、每阶段的要求与易错点见 `references/方法论.md`。

### 阶段 0：收集与确认任务信息

**在动手前，必须先向使用者主动提问一轮，把以下因任务而异的信息问清楚并确认，再开工。** 这些信息不可能预先打包，必须执行时收集。

**主动提问清单**（把这些问题列给使用者，等对方答复）：

1. **受测对象**：有哪些被测产品/系统，各自名称与版本？是否要用「对象 A / 对象 B」命名？A、B 分别对应谁（全文统一）。
2. **能力维度**：本次测评包含哪几个能力维度（常规能力、系统性能、深度洞察、数据分析、安全防护……）？每维度规模多大（多少任务/多少轮/多少用例）？
3. **子报告清单**：请使用者提供全部子报告文件，并说明每份对应哪个维度。
4. **风格参考报告**（可选）：是否有「只提供版式、不提供数据」的参考报告？没有的话，本 skill 内置了 `references/风格参考报告示例.pdf` 可作范本。
5. **额外要求**：保密属性（如「内部资料」）？交付格式（docx/pdf）？图表处理偏好？是否需要附录？
6. **口径约定**：是否允许跨维度合成统一分数（默认**不允许**）？差值方向（如「B-A」）固定吗？

> 拿不准的问题，先问人，不要猜。**没有确认的输入就开工，是后续返工的最大来源。** 宁可多问一轮，也不要带着错误假设往下走。

### 阶段 1：解析全部子报告

- 提取每份子报告的**文本内容**与**全部图片**。
- 关键：docx 提取文本时，务必同时提取 `word/media/` 下的图片；PDF 提取时同时导出嵌入图片。**图丢失是最高频的错误之一**。
- 记录每张图在原文档中的位置（出现在哪段文字之后），以便正确嵌回。

### 阶段 2：整合为统一结构

- 先确定综合报告的**整体骨架**（见 `references/报告骨架模板.md`）。
- 各子报告内容不是简单拼接，而是按统一章节格式重组；新增「总览」「执行摘要」「综合画像」等统领性章节，让报告成为一个整体。
- 术语、章节编号、图注编号、A/B 口径全文统一。
- 风格不一致的子报告，允许在「不改数值结论」前提下重组结构、统一章节格式。

### 阶段 3：表述打磨

- 把抽象、生僻、需要揣测的表述改写为「有专业感但可理解」的说法。
- 表格做间距/斑马纹等样式美化，不动数值。

### 阶段 4：强制自检（不可跳过）

- 拿成品回源比对，逐表逐句核对。用 `references/自检清单.md` 的五类清单逐项过。
- 发现「源报告自身的矛盾/瑕疵」时，忠实保留并列出，交由制作人决定是否统一，不擅自改。
- 把「拿不准的处理点」整理成清单，附在交付说明中让制作人判断。

### 阶段 5：交付

- 按要求的格式交付（docx/pdf 等）。
- 附一份「自检结论 + 待人工判断的点」说明，而不是只给成品。

## 硬性红线（MUST）

- MUST 忠实转述数值，不编造、不偏移、不擅自统一源报告自身的矛盾数值。
- MUST 不跨口径强行合成单一分数/结论。
- MUST 提取并保留子报告中的图片（除非经制作人确认删除）。
- MUST 在交付前执行完整自检。
- MUST 对拿不准的处理点列出交由制作人判断，而非擅自决定。

## 推荐做法（SHOULD）

- SHOULD 统一表述风格，让语言可读。
- SHOULD 美化表格样式（间距、斑马纹），提升可读性。
- SHOULD 统一章节编号、图注编号、术语口径。
- SHOULD 用「测评总览」「执行摘要」「综合画像」等统领章节增强整体性。

## 参考文件

- `references/方法论.md` — 五阶段详细方法论、每阶段要求与易错点、风格参考报告与风格不一致的处理。
- `references/自检清单.md` — 数值/逻辑/表述/图表/口径五类自检清单。
- `references/报告骨架模板.md` — 综合测评报告通用章节骨架。
- `references/风格参考报告示例.pdf` — 一份真实的版式范本，用于让 AI 直观理解目标版式。**只看它的风格/格式/排版，不参考其中任何数据或结论**（它来自一次无关的测评，其内容与任何新任务都无关系）。
- `references/worked-example.md` — 一次真实的 5 维测评汇总案例，作为方法论落地讲解素材。
